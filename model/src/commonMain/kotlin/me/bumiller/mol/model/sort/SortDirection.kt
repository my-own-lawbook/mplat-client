package me.bumiller.mol.model.sort

/**
 * Models direction a collection can be sorted by.
 */
enum class SortDirection {
    /**
     * Lower values first.
     */
    Ascending,

    /**
     * Higher values first.
     */
    Descending
}
